"use client";
import { FormEvent,useEffect,useState } from "react";
import { KiuboLocalDatabase,UserRole,createLocalBackup,getOpenCashSession,getTenantSettings,getWorkspaceContext,loadLocalDatabase,makeId,restoreLocalBackup,saveLocalDatabase } from "@/lib/local-store";
const roleLabel:Record<UserRole,string>={owner:"Propietario",admin:"Administrador",cashier:"Cajero",inventory:"Inventario",viewer:"Consulta"};
export function OperationsClient(){const[db,setDb]=useState<KiuboLocalDatabase|null>(null);const[closeAmount,setCloseAmount]=useState("");const[paymentDraft,setPaymentDraft]=useState<Record<string,string>>({});const[message,setMessage]=useState("Operación lista");const refresh=()=>setDb(loadLocalDatabase());useEffect(refresh,[]);if(!db)return <div className="loading-card">Preparando operación…</div>;const ctx=getWorkspaceContext(db),users=db.users.filter(u=>u.tenantId===ctx.tenantId),customers=db.customers.filter(c=>c.tenantId===ctx.tenantId),credits=db.credits.filter(c=>c.tenantId===ctx.tenantId&&c.branchId===ctx.branchId),openSession=getOpenCashSession(db,ctx.tenantId,ctx.branchId),settings=getTenantSettings(db,ctx.tenantId),persist=(next:KiuboLocalDatabase)=>{saveLocalDatabase(next);setDb({...next})},cashSummary=(()=>{if(!openSession)return{sales:0,income:0,out:0,expected:0};const sales=db.sales.filter(s=>s.tenantId===ctx.tenantId&&s.branchId===ctx.branchId&&s.payment==="cash"&&new Date(s.createdAt)>=new Date(openSession.openedAt)).reduce((n,s)=>n+s.total,0),moves=db.cashMovements.filter(m=>m.sessionId===openSession.id),income=moves.filter(m=>m.type==="in").reduce((n,m)=>n+m.amount,0),out=moves.filter(m=>m.type==="out").reduce((n,m)=>n+m.amount,0);return{sales,income,out,expected:openSession.openingAmount+sales+income-out}})();const addUser=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget),name=String(f.get("name")||"").trim(),email=String(f.get("email")||"").trim().toLowerCase(),pin=String(f.get("pin")||"").trim();if(!name||!email||!/^[0-9]{4}$/.test(pin)){setMessage("Completa usuario, correo y PIN de 4 dígitos");return}if(db.users.some(u=>u.email.toLowerCase()===email)){setMessage("Ese correo ya existe");return}db.users.unshift({id:makeId("user"),tenantId:ctx.tenantId,name,email,role:String(f.get("role")||"cashier") as UserRole,active:true,pin,platformAdmin:false,createdAt:new Date().toISOString()});db.tenants=db.tenants.map(t=>t.id===ctx.tenantId?{...t,users:db.users.filter(u=>u.tenantId===ctx.tenantId&&u.active).length}:t);persist(db);e.currentTarget.reset();setMessage("Usuario creado")};const toggleUser=(id:string)=>{db.users=db.users.map(u=>u.id===id?{...u,active:!u.active}:u);db.tenants=db.tenants.map(t=>t.id===ctx.tenantId?{...t,users:db.users.filter(u=>u.tenantId===ctx.tenantId&&u.active).length}:t);persist(db)};const openCash=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();if(getOpenCashSession(db,ctx.tenantId,ctx.branchId))return;const f=new FormData(e.currentTarget),amount=Number(f.get("opening")||0);db.cashSessions.unshift({id:makeId("cash"),tenantId:ctx.tenantId,branchId:ctx.branchId,openingAmount:Number(amount.toFixed(2)),status:"open",openedAt:new Date().toISOString(),openedBy:ctx.user?.name??"Usuario"});persist(db);e.currentTarget.reset();setMessage("Caja abierta")};const addMovement=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const session=getOpenCashSession(db,ctx.tenantId,ctx.branchId);if(!session)return;const f=new FormData(e.currentTarget),amount=Number(f.get("amount")||0);if(amount<=0)return;db.cashMovements.unshift({id:makeId("movement"),tenantId:ctx.tenantId,branchId:ctx.branchId,sessionId:session.id,type:String(f.get("type")) as "in"|"out",amount:Number(amount.toFixed(2)),reason:String(f.get("reason")||"Movimiento"),createdAt:new Date().toISOString()});persist(db);e.currentTarget.reset();setMessage("Movimiento de caja registrado")};const closeCash=()=>{const session=getOpenCashSession(db,ctx.tenantId,ctx.branchId),amount=Number(closeAmount);if(!session||!Number.isFinite(amount))return;db.cashSessions=db.cashSessions.map(s=>s.id===session.id?{...s,status:"closed",closingAmount:Number(amount.toFixed(2)),closedAt:new Date().toISOString()}:s);persist(db);setCloseAmount("");setMessage("Caja cerrada")};const payCredit=(creditId:string)=>{const amount=Number(paymentDraft[creditId]);if(!Number.isFinite(amount)||amount<=0)return;const credit=db.credits.find(c=>c.id===creditId);if(!credit)return;const applied=Math.min(amount,credit.balance);db.creditPayments.unshift({id:makeId("credit-payment"),tenantId:ctx.tenantId,branchId:ctx.branchId,creditId,amount:Number(applied.toFixed(2)),method:"cash",createdAt:new Date().toISOString()});db.credits=db.credits.map(c=>c.id===creditId?{...c,balance:Number((c.balance-applied).toFixed(2)),status:c.balance-applied<=.001?"paid":"open"}:c);persist(db);setPaymentDraft(v=>({...v,[creditId]:""}));setMessage("Abono registrado")};const saveSettings=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget),next={...settings,tradeName:String(f.get("tradeName")||""),legalName:String(f.get("legalName")||""),ruc:String(f.get("ruc")||""),establishment:String(f.get("establishment")||"001"),emissionPoint:String(f.get("emissionPoint")||"001"),receiptFooter:String(f.get("receiptFooter")||""),accent:String(f.get("accent")||"#ff5b55"),requireCashSession:f.get("requireCashSession")==="on",allowCredit:f.get("allowCredit")==="on"};db.settings=db.settings.filter(s=>s.tenantId!==ctx.tenantId);db.settings.push(next);persist(db);setMessage("Configuración guardada")};const downloadBackup=()=>{const blob=new Blob([createLocalBackup()],{type:"application/json"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download=`kiubo-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(url)};const restore=(file?:File)=>{if(!file)return;const reader=new FileReader();reader.onload=()=>{try{restoreLocalBackup(String(reader.result||""));refresh();setMessage("Respaldo restaurado")}catch{alert("El archivo no es un respaldo válido de KIUBO")}};reader.readAsText(file)};return <><div className="workspace-banner"><div><span>NEGOCIO</span><strong>{ctx.tenant?.name}</strong></div><div><span>SUCURSAL</span><strong>{ctx.branch?.code} · {ctx.branch?.name}</strong></div><div><span>ESTADO</span><strong>{message}</strong></div></div><header className="topbar"><div><span className="eyebrow">KIUBO OPERACIÓN</span><h1>Equipo, caja, fiados y configuración</h1></div><button className="button secondary" onClick={downloadBackup}>Descargar respaldo</button></header><section className="ops-grid"><article className="panel"><div className="panel-head"><div><span className="eyebrow">EQUIPO</span><h3>Usuarios del negocio</h3></div><span className="pill">{users.filter(u=>u.active).length} activos</span></div><form className="ops-form ops-form-3" onSubmit={addUser}><input name="name" placeholder="Nombre" required/><input name="email" type="email" placeholder="Correo" required/><select name="role" defaultValue="cashier">{Object.entries(roleLabel).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select><input name="pin" inputMode="numeric" pattern="[0-9]{4}" maxLength={4} placeholder="PIN" required/><button className="button primary" type="submit">Agregar usuario</button></form><div className="ops-list">{users.map(u=><div className="ops-row" key={u.id}><div><strong>{u.name}</strong><span>{u.email} · {roleLabel[u.role]}{u.platformAdmin?" · KIUBO Admin":""}</span></div>{!u.platformAdmin&&<button className={u.active?"mini-action danger":"mini-action resume"} onClick={()=>toggleUser(u.id)}>{u.active?"Desactivar":"Activar"}</button>}</div>)}</div></article><article className="panel"><div className="panel-head"><div><span className="eyebrow">CAJA · {ctx.branch?.name}</span><h3>{openSession?"Caja abierta":"Caja cerrada"}</h3></div><span className={openSession?"status status-active":"status status-suspended"}>{openSession?"ABIERTA":"CERRADA"}</span></div>{!openSession?<form className="ops-form" onSubmit={openCash}><label>Fondo inicial<input name="opening" type="number" min="0" step="0.01" defaultValue="0"/></label><button className="button primary" type="submit">Abrir caja</button></form>:<><div className="cash-metrics"><div><span>Inicial</span><strong>${openSession.openingAmount.toFixed(2)}</strong></div><div><span>Ventas efectivo</span><strong>${cashSummary.sales.toFixed(2)}</strong></div><div><span>Esperado</span><strong>${cashSummary.expected.toFixed(2)}</strong></div></div><form className="ops-form ops-form-3" onSubmit={addMovement}><select name="type"><option value="in">Ingreso</option><option value="out">Egreso</option></select><input name="amount" type="number" min="0.01" step="0.01" placeholder="Monto" required/><input name="reason" placeholder="Motivo" required/><button className="button secondary" type="submit">Registrar</button></form><div className="close-cash"><input value={closeAmount} onChange={e=>setCloseAmount(e.target.value)} type="number" min="0" step="0.01" placeholder="Efectivo contado"/><button className="button primary" onClick={closeCash}>Cerrar caja</button></div><small className="ops-note">La caja queda separada por sucursal.</small></>}</article></section><section className="ops-grid"><article className="panel"><div className="panel-head"><div><span className="eyebrow">FIADOS · {ctx.branch?.name}</span><h3>Cuentas por cobrar</h3></div><span className="pill">${credits.filter(c=>c.status==="open").reduce((n,c)=>n+c.balance,0).toFixed(2)} pendiente</span></div><div className="ops-list">{credits.length===0?<p className="empty-cart">Cuando cobres una venta como Fiado aparecerá aquí.</p>:credits.map(c=>{const customer=customers.find(x=>x.id===c.customerId);return <div className="credit-row" key={c.id}><div><strong>{customer?.name??"Cliente"}</strong><span>{c.description} · Original ${c.originalAmount.toFixed(2)}</span></div><b>${c.balance.toFixed(2)}</b>{c.status==="open"?<><input value={paymentDraft[c.id]??""} onChange={e=>setPaymentDraft(v=>({...v,[c.id]:e.target.value}))} type="number" min="0.01" step="0.01" placeholder="Abono"/><button className="button secondary compact" onClick={()=>payCredit(c.id)}>Abonar</button></>:<span className="status status-active">PAGADO</span>}</div>})}</div></article><form className="panel ops-settings" onSubmit={saveSettings}><div className="panel-head"><div><span className="eyebrow">NEGOCIO</span><h3>Configuración general</h3></div></div><div className="settings-grid"><label>Nombre comercial<input name="tradeName" defaultValue={settings.tradeName}/></label><label>Razón social<input name="legalName" defaultValue={settings.legalName}/></label><label>RUC<input name="ruc" defaultValue={settings.ruc}/></label><label>Establecimiento<input name="establishment" defaultValue={settings.establishment}/></label><label>Punto de emisión<input name="emissionPoint" defaultValue={settings.emissionPoint}/></label><label>Color de marca<input name="accent" type="color" defaultValue={settings.accent}/></label><label className="settings-wide">Pie de comprobante<input name="receiptFooter" defaultValue={settings.receiptFooter}/></label><label className="switch-row"><input name="requireCashSession" type="checkbox" defaultChecked={settings.requireCashSession}/>Exigir caja abierta para efectivo</label><label className="switch-row"><input name="allowCredit" type="checkbox" defaultChecked={settings.allowCredit}/>Permitir fiado</label></div><button className="button primary" type="submit">Guardar configuración</button><div className="backup-restore"><span>Restaurar respaldo local</span><input type="file" accept="application/json" onChange={e=>restore(e.target.files?.[0])}/></div></form></section></>}
