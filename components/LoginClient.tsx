"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
import { KiuboMark } from "./Logo";
import { getAuthProvider } from "@/lib/auth-provider";
import { homeForRole } from "@/lib/permissions";
export function LoginClient(){const router=useRouter(),auth=getAuthProvider(),cloud=auth.mode==="supabase";const[error,setError]=useState(""),[busy,setBusy]=useState(false);const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget),email=String(f.get("email")||"").trim(),pin=String(f.get("pin")||"").trim(),password=String(f.get("password")||"");const result=await auth.signIn({email,pin,password});setBusy(false);if(!result.ok||!result.user){setError(result.error||"No se pudo iniciar sesión");return}router.replace(result.user.platformAdmin?"/control":homeForRole(result.user.role))};return <main className="ref-login-shell"><section className="ref-login-brand"><KiuboMark/><div className="ref-login-copy"><span className="public-kicker">TU NEGOCIO, SIMPLE</span><h1>Todo<br/><em>en orden.</em></h1><p>Ventas, inventario, clientes y control en una plataforma pensada para que tu equipo pueda empezar sin complicaciones.</p></div><div className="ref-login-benefits"><article><i className="blue">◎</i><div><b>Fácil de usar</b><span>Interfaz clara desde el primer día.</span></div></article><article><i className="orange">⌘</i><div><b>Todo en uno</b><span>Vende, controla y revisa tu negocio.</span></div></article><article><i className="coral">↗</i><div><b>Información al instante</b><span>Datos del negocio en un solo lugar.</span></div></article><article><i className="navy">◇</i><div><b>Seguro por diseño</b><span>{cloud?"Acceso protegido con Auth cloud y aislamiento por negocio.":"Preview local antes de activar Auth cloud."}</span></div></article></div></section><section className="ref-login-area"><div className="ref-login-shape s1"/><div className="ref-login-shape s2"/><form className="ref-login-card" onSubmit={submit}><KiuboMark/><div><span className="eyebrow">¡BIENVENIDO!</span><h2>Inicia sesión para continuar</h2><p>Entra con el acceso asignado a tu negocio.</p></div>{error&&<div className="login-error">{error}</div>}<label>Correo electrónico<input name="email" type="email" placeholder="tu@negocio.com" required autoFocus/></label>{cloud?<label>Contraseña<input name="password" type="password" autoComplete="current-password" minLength={8} placeholder="Tu contraseña" required/></label>:<label>PIN<input name="pin" type="password" inputMode="numeric" pattern="[0-9]{4}" maxLength={4} placeholder="••••" required/></label>}<button className="button ref-blue-button" type="submit" disabled={busy}>{busy?"Entrando…":"Iniciar sesión"}</button>{cloud?<small className="ref-login-demo">KIUBO Cloud · acceso real del negocio</small>:<small className="ref-login-demo">Preview: admin@kiubo.local / caja@kiubo.local · PIN 1234</small>}</form></section></main>}
