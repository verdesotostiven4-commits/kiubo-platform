"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
import { KiuboMark } from "./Logo";
import { getAuthProvider } from "@/lib/auth-provider";
import { homeForRole } from "@/lib/permissions";
export function LoginClient(){const router=useRouter();const[error,setError]=useState("");const[busy,setBusy]=useState(false);const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget),email=String(f.get("email")||"").trim(),pin=String(f.get("pin")||"").trim();const result=await getAuthProvider().signIn({email,pin});setBusy(false);if(!result.ok||!result.user){setError(result.error||"No se pudo iniciar sesión");return}router.replace(result.user.platformAdmin?"/control":homeForRole(result.user.role))};return <main className="login-shell"><form className="login-card" onSubmit={submit}><KiuboMark/><div><span className="eyebrow">BIENVENIDO</span><h1>Tu negocio empieza aquí.</h1><p>Accede a tu empresa y sucursal con tu rol asignado.</p></div>{error&&<div className="login-error">{error}</div>}<label>Correo<input name="email" type="email" placeholder="tu@negocio.com" required autoFocus/></label><label>PIN<input name="pin" type="password" inputMode="numeric" pattern="[0-9]{4}" maxLength={4} placeholder="••••" required/></label><button className="button primary" type="submit" disabled={busy}>{busy?"Entrando…":"Entrar"}</button><small>Demo local: admin@kiubo.local o caja@kiubo.local · PIN 1234. La UI ya usa un contrato de Auth reemplazable; producción usará Auth cloud.</small></form></main>}
