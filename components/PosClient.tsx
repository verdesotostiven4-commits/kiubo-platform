"use client";
import { useEffect,useState } from "react";
import { CustomerRecord,SaleRecord,TenantProduct,getOpenCashSession,getTenantSettings,getWorkspaceContext,loadLocalDatabase,makeId,saveLocalDatabase } from "@/lib/local-store";
type CartLine=TenantProduct&{qty:number};type Payment=SaleRecord["payment"];
const paymentLabel:Record<Payment,string>={cash:"Efectivo",transfer:"Transferencia",mixed:"Mixto",credit:"Fiado"};
export function PosClient(){const[db,setDb]=useState<ReturnType<typeof loadLocalDatabase>|null>(null);const[cart,setCart]=useState<CartLine[]>([]);const[query,setQuery]=useState("");const[payment,setPayment]=useState<Payment>("cash");const[customerId,setCustomerId]=useState("");const[message,setMessage]=useState("Listo para vender");const refresh=()=>setDb(loadLocalDatabase());useEffect(refresh,[]);if(!db)return <div className="loading-card">Preparando POS…</div>;const ctx=getWorkspaceContext(db),tenant=ctx.tenant,branch=ctx.branch,products=db.tenantProducts.filter(p=>p.tenantId===ctx.tenantId&&p.branchId===ctx.branchId&&p.active),customers:CustomerRecord[]=db.customers.filter(c=>c.tenantId===ctx.tenantId),filtered=products.filter(p=>`${p.name} ${p.barcode} ${p.price}`.toLowerCase().includes(query.toLowerCase())),total=cart.reduce((sum,line)=>sum+line.price*line.qty,0),blocked=tenant?.status==="suspended";const add=(product:TenantProduct)=>{if(product.stock<=0){setMessage(`${product.name} sin stock`);return}setCart(current=>{const found=current.find(line=>line.id===product.id);if(found){if(found.qty>=product.stock){setMessage(`Stock máximo: ${product.stock}`);return current}return current.map(line=>line.id===product.id?{...line,qty:line.qty+1}:line)}return[...current,{...product,qty:1}]});setMessage(`${product.name} agregado`)};const changeQty=(id:string,delta:number)=>setCart(current=>current.map(line=>line.id===id?{...line,qty:Math.min(line.stock,line.qty+delta)}:line).filter(line=>line.qty>0));const checkout=()=>{if(!cart.length){setMessage("Agrega al menos un producto");return}const next=loadLocalDatabase(),workspace=getWorkspaceContext(next),settings=getTenantSettings(next,workspace.tenantId);if(workspace.tenant?.status==="suspended"){setMessage("La licencia está suspendida");return}if(!workspace.branchId){setMessage("Selecciona una sucursal");return}if(payment==="credit"&&!settings.allowCredit){setMessage("El fiado está desactivado");return}if(payment==="credit"&&!customerId){setMessage("Selecciona un cliente para registrar el fiado");return}if((payment==="cash"||payment==="mixed")&&settings.requireCashSession&&!getOpenCashSession(next,workspace.tenantId,workspace.branchId)){setMessage("Debes abrir caja en esta sucursal antes de cobrar");return}for(const line of cart){const current=next.tenantProducts.find(p=>p.id===line.id&&p.tenantId===workspace.tenantId&&p.branchId===workspace.branchId);if(!current||current.stock<line.qty){setMessage(`Revisa stock de ${line.name}`);refresh();return}}const now=new Date().toISOString(),sale:SaleRecord={id:makeId("sale"),tenantId:workspace.tenantId,branchId:workspace.branchId,customerId:customerId||undefined,total:Number(total.toFixed(2)),payment,items:cart.map(line=>({productId:line.id,name:line.name,qty:line.qty,unitPrice:line.price,unitCost:line.cost})),createdAt:now};next.sales.unshift(sale);if(payment==="credit"&&customerId)next.credits.unshift({id:makeId("credit"),tenantId:workspace.tenantId,branchId:workspace.branchId,customerId,saleId:sale.id,description:`Venta ${sale.id.slice(-8)}`,originalAmount:sale.total,balance:sale.total,status:"open",createdAt:now});for(const line of cart){const current=next.tenantProducts.find(p=>p.id===line.id)!;const previous=current.stock,newStock=previous-line.qty;next.stockMovements.unshift({id:makeId("stock"),tenantId:workspace.tenantId,branchId:workspace.branchId,productId:current.id,type:"sale",quantity:-line.qty,previousStock:previous,newStock,reference:sale.id,createdAt:now});current.stock=newStock}saveLocalDatabase(next);setDb(next);setCart([]);setCustomerId("");setMessage(`Venta guardada · ${paymentLabel[payment]} · $${sale.total.toFixed(2)}`)};if(blocked)return <div className="license-block"><div className="hero-mini">K</div><span className="eyebrow">LICENCIA SUSPENDIDA</span><h2>{tenant?.name??"Este negocio"} está en modo consulta.</h2><p>No se borra información ni se permiten nuevas ventas hasta reactivar la licencia desde KIUBO Control.</p></div>;return <><div className="workspace-banner"><div><span>NEGOCIO</span><strong>{tenant?.name??"Negocio"}</strong></div><div><span>SUCURSAL</span><strong>{branch?.code} · {branch?.name}</strong></div><div><span>ESTADO</span><strong>{tenant?.status.toUpperCase()}</strong></div></div><div className="pos-grid"><section className="pos-products"><div className="pos-message">{message}</div><input className="search-input" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar por nombre, código o precio…"/><div className="product-results">{filtered.length?filtered.map(product=><button className="pos-product" key={product.id} onClick={()=>add(product)}><div className="product-icon">{product.name.charAt(0)}</div><div><strong>{product.name}</strong><span>{product.barcode} · Stock {product.stock}</span></div><b>${product.price.toFixed(2)}</b></button>):<p className="empty-cart">Esta sucursal todavía no tiene productos. Agrégalos desde Catalog.</p>}</div></section><aside className="cart-panel"><div className="cart-head"><span className="eyebrow">VENTA ACTUAL</span><h2>{cart.reduce((n,l)=>n+l.qty,0)} productos</h2></div><select className="pos-customer-select" value={customerId} onChange={e=>setCustomerId(e.target.value)}><option value="">Cliente opcional</option>{customers.map(c=><option key={c.id} value={c.id}>{c.name} · {c.identification}</option>)}</select><div className="cart-lines">{cart.length===0?<p className="empty-cart">Escanea o toca un producto para comenzar.</p>:cart.map(line=><div className="cart-line interactive" key={line.id}><div><strong>{line.name}</strong><span>${line.price.toFixed(2)} c/u</span></div><div className="qty"><button onClick={()=>changeQty(line.id,-1)}>−</button><b>{line.qty}</b><button onClick={()=>changeQty(line.id,1)}>+</button></div><strong>${(line.price*line.qty).toFixed(2)}</strong></div>)}</div><div className="cart-total"><span>Total</span><strong>${total.toFixed(2)}</strong></div><div className="payment-grid">{(Object.keys(paymentLabel) as Payment[]).map(method=><button key={method} className={payment===method?"selected":""} onClick={()=>setPayment(method)}>{paymentLabel[method]}</button>)}</div><button className="button primary checkout" onClick={checkout}>Cobrar ${total.toFixed(2)}</button><p className="cart-note">Stock, caja, venta y kardex se guardan en la sucursal activa. Clientes y configuración pertenecen al negocio.</p></aside></div></>}
