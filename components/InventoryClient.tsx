"use client";
import { FormEvent,useEffect,useState } from "react";
import { KiuboLocalDatabase,getWorkspaceContext,loadLocalDatabase,makeId,saveLocalDatabase } from "@/lib/local-store";
const movementLabel={sale:"Venta",purchase:"Compra",adjustment_in:"Ajuste +",adjustment_out:"Ajuste -"} as const;
export function InventoryClient(){const[db,setDb]=useState<KiuboLocalDatabase|null>(null);const[message,setMessage]=useState("Inventario listo");useEffect(()=>setDb(loadLocalDatabase()),[]);if(!db)return <div className="loading-card">Preparando inventario…</div>;const ctx=getWorkspaceContext(db),products=db.tenantProducts.filter(p=>p.tenantId===ctx.tenantId&&p.branchId===ctx.branchId&&p.active),suppliers=db.suppliers.filter(s=>s.tenantId===ctx.tenantId),purchases=db.purchases.filter(p=>p.tenantId===ctx.tenantId&&p.branchId===ctx.branchId),movements=db.stockMovements.filter(m=>m.tenantId===ctx.tenantId&&m.branchId===ctx.branchId),metrics={units:products.reduce((n,p)=>n+p.stock,0),value:products.reduce((n,p)=>n+p.stock*p.cost,0),low:products.filter(p=>p.stock<=5).length,suppliers:suppliers.length},persist=(next:KiuboLocalDatabase)=>{saveLocalDatabase(next);setDb({...next})};const addSupplier=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget),name=String(f.get("name")||"").trim();if(!name)return;db.suppliers.unshift({id:makeId("supplier"),tenantId:ctx.tenantId,identification:String(f.get("identification")||"").trim(),name,email:String(f.get("email")||"").trim(),phone:String(f.get("phone")||"").trim(),address:String(f.get("address")||"").trim(),createdAt:new Date().toISOString()});persist(db);e.currentTarget.reset();setMessage("Proveedor guardado para el negocio")};const addPurchase=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget),productId=String(f.get("productId")||""),supplierId=String(f.get("supplierId")||""),qty=Number(f.get("qty")||0),cost=Number(f.get("cost")||0),product=db.tenantProducts.find(p=>p.id===productId&&p.tenantId===ctx.tenantId&&p.branchId===ctx.branchId);if(!product||!supplierId||qty<=0||cost<0)return;const now=new Date().toISOString(),previous=product.stock,newStock=previous+qty,purchaseId=makeId("purchase");db.purchases.unshift({id:purchaseId,tenantId:ctx.tenantId,branchId:ctx.branchId,supplierId,total:Number((qty*cost).toFixed(2)),items:[{productId:product.id,name:product.name,qty,unitCost:Number(cost.toFixed(4))}],createdAt:now});db.stockMovements.unshift({id:makeId("stock"),tenantId:ctx.tenantId,branchId:ctx.branchId,productId:product.id,type:"purchase",quantity:qty,previousStock:previous,newStock,reference:purchaseId,createdAt:now});db.tenantProducts=db.tenantProducts.map(p=>p.id===product.id?{...p,stock:newStock,cost:Number(cost.toFixed(4))}:p);persist(db);e.currentTarget.reset();setMessage(`Compra registrada · ${product.name} +${qty}`)};const adjust=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget),productId=String(f.get("productId")||""),kind=String(f.get("kind")||"in") as "in"|"out",reason=String(f.get("reason")||"Ajuste").trim(),qty=Number(f.get("qty")||0),product=db.tenantProducts.find(p=>p.id===productId&&p.tenantId===ctx.tenantId&&p.branchId===ctx.branchId);if(!product||qty<=0)return;const signed=kind==="in"?qty:-qty,newStock=Math.max(0,product.stock+signed),actualDelta=newStock-product.stock;db.stockMovements.unshift({id:makeId("stock"),tenantId:ctx.tenantId,branchId:ctx.branchId,productId:product.id,type:actualDelta>=0?"adjustment_in":"adjustment_out",quantity:actualDelta,previousStock:product.stock,newStock,reference:reason,createdAt:new Date().toISOString()});db.tenantProducts=db.tenantProducts.map(p=>p.id===product.id?{...p,stock:newStock}:p);persist(db);e.currentTarget.reset();setMessage(`Stock ajustado · ${product.name}`)};return <><div className="workspace-banner"><div><span>NEGOCIO</span><strong>{ctx.tenant?.name}</strong></div><div><span>SUCURSAL</span><strong>{ctx.branch?.code} · {ctx.branch?.name}</strong></div><div><span>ESTADO</span><strong>{message}</strong></div></div><header className="topbar"><div><span className="eyebrow">KIUBO INVENTARIO</span><h1>Stock, compras y proveedores</h1></div></header><section className="metric-grid"><article className="metric-card"><span>Unidades</span><strong>{metrics.units}</strong><small>Stock de sucursal</small></article><article className="metric-card"><span>Valor a costo</span><strong>${metrics.value.toFixed(2)}</strong><small>Inventario estimado</small></article><article className="metric-card"><span>Stock bajo</span><strong>{metrics.low}</strong><small>5 unidades o menos</small></article><article className="metric-card"><span>Proveedores</span><strong>{metrics.suppliers}</strong><small>Compartidos por negocio</small></article></section><section className="inventory-grid"><article className="panel"><div className="panel-head"><div><span className="eyebrow">PRODUCTOS</span><h3>Inventario actual</h3></div></div><div className="inventory-list">{products.length?products.map(p=>{const margin=p.price?((p.price-p.cost)/p.price)*100:0;return <div className="inventory-row" key={p.id}><div><strong>{p.name}</strong><span>{p.barcode} · costo ${p.cost.toFixed(2)} · venta ${p.price.toFixed(2)}</span></div><b className={p.stock<=5?"stock-low":""}>{p.stock}</b><small>{margin.toFixed(0)}% margen bruto</small></div>}):<p className="empty-cart">Esta sucursal aún no tiene productos.</p>}</div></article><article className="panel"><span className="eyebrow">COMPRA RÁPIDA</span><h3>Ingreso de mercadería</h3><form className="inventory-form" onSubmit={addPurchase}><label>Proveedor<select name="supplierId" required defaultValue=""><option value="" disabled>Selecciona</option>{suppliers.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label>Producto<select name="productId" required defaultValue=""><option value="" disabled>Selecciona</option>{products.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><div className="inventory-inline"><label>Cantidad<input name="qty" type="number" min="1" step="1" required/></label><label>Costo unitario<input name="cost" type="number" min="0" step="0.0001" required/></label></div><button className="button primary" type="submit" disabled={!suppliers.length||!products.length}>Registrar compra</button></form></article></section><section className="inventory-grid"><article className="panel"><div className="panel-head"><div><span className="eyebrow">PROVEEDORES</span><h3>Directorio del negocio</h3></div><span className="pill">{suppliers.length}</span></div><form className="inventory-form supplier-form" onSubmit={addSupplier}><input name="name" placeholder="Nombre / razón social" required/><input name="identification" placeholder="RUC / identificación"/><input name="phone" placeholder="Teléfono"/><input name="email" type="email" placeholder="Correo"/><input name="address" placeholder="Dirección"/><button className="button secondary" type="submit">Guardar proveedor</button></form><div className="supplier-list">{suppliers.map(s=><div className="supplier-row" key={s.id}><strong>{s.name}</strong><span>{s.identification||"Sin RUC"} · {s.phone||"Sin teléfono"}</span></div>)}</div></article><article className="panel"><span className="eyebrow">AJUSTE CONTROLADO</span><h3>Corregir stock de sucursal</h3><form className="inventory-form" onSubmit={adjust}><label>Producto<select name="productId" required defaultValue=""><option value="" disabled>Selecciona</option>{products.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><div className="inventory-inline"><label>Tipo<select name="kind"><option value="in">Entrada</option><option value="out">Salida</option></select></label><label>Cantidad<input name="qty" type="number" min="1" step="1" required/></label></div><label>Motivo<input name="reason" placeholder="Conteo físico, daño, corrección…" required/></label><button className="button secondary" type="submit">Aplicar ajuste</button></form></article></section><section className="inventory-grid"><article className="panel"><div className="panel-head"><div><span className="eyebrow">KARDEX</span><h3>Movimientos recientes</h3></div></div><div className="movement-list">{movements.length===0?<p className="empty-cart">Todavía no hay movimientos en esta sucursal.</p>:movements.slice(0,30).map(m=>{const product=products.find(p=>p.id===m.productId);return <div className="movement-row" key={m.id}><div><strong>{product?.name??"Producto"}</strong><span>{movementLabel[m.type]} · {new Date(m.createdAt).toLocaleString("es-EC")}</span></div><b className={m.quantity>=0?"movement-in":"movement-out"}>{m.quantity>=0?"+":""}{m.quantity}</b><small>{m.previousStock} → {m.newStock}</small></div>})}</div></article><article className="panel"><div className="panel-head"><div><span className="eyebrow">COMPRAS</span><h3>Historial de sucursal</h3></div><span className="pill">{purchases.length}</span></div><div className="movement-list">{purchases.length===0?<p className="empty-cart">Registra la primera compra arriba.</p>:purchases.slice(0,20).map(p=>{const supplier=suppliers.find(s=>s.id===p.supplierId);return <div className="movement-row" key={p.id}><div><strong>{supplier?.name??"Proveedor"}</strong><span>{p.items.map(i=>`${i.qty}× ${i.name}`).join(" · ")}</span></div><b>${p.total.toFixed(2)}</b><small>{new Date(p.createdAt).toLocaleDateString("es-EC")}</small></div>})}</div></article></section></>}
