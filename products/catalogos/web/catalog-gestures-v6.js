(()=>{'use strict';const host=()=>document.querySelector('#sheetHost');let active=null;function bind(sheet){if(!sheet||sheet.dataset.pullDismiss)return;sheet.dataset.pullDismiss='1';const scroll=sheet.querySelector('.v6-sheet-scroll');if(!scroll)return;let startY=0,lastY=0,tracking=false,moved=false;const reset=()=>{tracking=false;moved=false;sheet.classList.remove('dragging');sheet.style.transform=''};scroll.addEventListener('pointerdown',e=>{if(e.pointerType==='mouse'||scroll.scrollTop>1)return;startY=lastY=e.clientY;tracking=true;moved=false},{passive:true});scroll.addEventListener('pointermove',e=>{if(!tracking)return;lastY=e.clientY;const dy=lastY-startY;if(dy<=0){tracking=false;return}if(dy>7)moved=true;if(!moved)return;e.preventDefault();sheet.classList.add('dragging');sheet.style.transform=`translate3d(-50%,${Math.min(dy,320)}px,0)`},{passive:false});const end=()=>{if(!tracking)return;const dy=Math.max(0,lastY-startY);tracking=false;if(dy>92){sheet.querySelector('.v6-sheet-close')?.click()}else reset()};scroll.addEventListener('pointerup',end,{passive:true});scroll.addEventListener('pointercancel',reset,{passive:true})}function scan(){const sheet=host()?.querySelector('.v6-sheet');if(sheet&&sheet!==active){active=sheet;bind(sheet)}}const start=()=>{new MutationObserver(scan).observe(document.body,{childList:true,subtree:true});scan()};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start()})();