/* Hakuna Matata · Brand asset registry
   Add public logo URLs here as they are approved/uploaded.
   Keys are normalized brand names (lowercase, accents removed, spaces -> hyphens).
   Catalog 10.3 automatically falls back to a clean branded text tile when a logo is missing. */
window.KIUBO_BRAND_ASSETS=Object.freeze({
  /* Example:
  'coca-cola':'https://.../coca-cola.png',
  'toni':'https://.../toni.png'
  */
});
