import Link from "next/link";
import { PublicHeader } from "@/components/PublicHeader";
import { PLAN_CATALOG,type CommercialPlan } from "@/lib/entitlements";
const order:CommercialPlan[]=["Start","Pro","Custom"];
export default function PricingPage(){return <main className="marketing-shell"><PublicHeader/><section className="public-section public-pricing-head"><span className="public-kicker">PRECIOS DE PREVIEW COMERCIAL</span><h1>Empieza simple. Sube de nivel cuando tu negocio lo necesite.</h1><p>Todos los negocios usan la misma plataforma KIUBO. El plan define módulos y alcance; nunca creamos un programa distinto por cliente.</p></section><section className="public-section public-price-grid">{order.map(plan=>{const data=PLAN_CATALOG[plan];return <article className={`public-price-card ${plan==="Pro"?"featured":""}`} key={plan}><span>{plan==="Pro"?"MÁS COMPLETO PARA CRECER":plan==="Start"?"PARA EMPEZAR":"CONFIGURACIÓN ESPECIAL"}</span><h2>KIUBO {plan}</h2><strong>{data.price}<small>/mes*</small></strong><p>{data.tagline}</p><ul>{data.features.map(item=><li key={item}>{item}</li>)}</ul><Link href="/demo" className="button primary">{plan==="Custom"?"Hablar con KIUBO":"Probar 14 días"}</Link></article>})}</section><section className="public-section public-note"><strong>Factura electrónica se activa por separado.</strong><span>El trial normal es de 14 días. Los pilotos fundadores pueden recibir condiciones especiales. Los precios mostrados siguen siendo provisionales hasta cerrar costos de infraestructura, soporte y facturación.</span></section></main>}
